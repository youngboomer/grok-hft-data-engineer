# Mini Order Book Demo

A very small but correct demonstration of snapshot + delta application with sequence checking.

Run:

```bash
cargo run --release
```

This is the foundation you will expand in a real implementation to handle full depth, multiple symbols, and fast top-N queries.
